from django.conf.urls import include, url

urlpatterns = [
    # Create your urls here.
    # Examples:
    # url(r'^$', 'thesis_project.views.home', name='home'),
    # url(r'^blog/', include('blog.urls')),
]